import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  root: {
    padding: 40
  },
  input: {
    borderColor: 'black',
    borderWidth: 1,
    marginBottom: 10,
  },
});

export default styles;