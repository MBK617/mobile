import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  root: {
    padding: 40,
  },
  error: {
    textAlign: 'center',
    color: '#ff0000',
    width: 250,
    marginBottom: 10,
  }
});

export default styles;