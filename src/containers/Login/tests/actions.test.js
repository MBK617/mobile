// import * as actions from '../actions'

describe('actions', () => {
  // it('should create an action to increment the count', () => {    
  //   const expectedAction = {
  //     type: 'INCREMENT'
  //   }
  //   expect(actions.increment()).toEqual(expectedAction)
  // })

  // it('should create an action to dencrement the count', () => {    
  //   const expectedAction = {
  //     type: 'DECREMENT'
  //   }
  //   expect(actions.decrement()).toEqual(expectedAction)
  // })

  // it('should create an action to asynchronously increment the count', () => {    
  //   const expectedAction = {
  //     type: 'INCREMENT_ASYNC'
  //   }
  //   expect(actions.incrementAsync()).toEqual(expectedAction)
  // })

  // it('should create an action to signal that aynchronous increment was completed and successful', () => {    
  //   const expectedAction = {
  //     type: 'INCREMENT_ASYNC_SUCCESS'
  //   }
  //   expect(actions.incrementAsyncSuccess()).toEqual(expectedAction)
  // })
})